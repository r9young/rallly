# Simple Calendar Example

This folder contains a minimal calendar component that mirrors the grid logic of
`apps/web`'s `month-calendar`. It reuses the `useHeadlessDatePicker` hook and
helper functions like `removeAllOptionsForDay` to keep the behaviour similar,
but strips out additional features. Open `SimpleCalendar.tsx` to see the
implementation.
